import Image from "next/image";
import Portallanding from "../../PortalLanding/page"
// import useSession
export default function Home() {
  // const {data:session,status} = useSession({required:true})
  return (
    <main >
      <Portallanding />
    </main>
  );
}
